spring:
  datasource:
    url: jdbc:postgresql://89.23.102.97:5432/oko_api?sslmode=prefer
    username: oko_user
    password: Mx@:.s7T@#YUrg
  hikari:
    maximum-pool-size: 5
    minimum-idle: 0
    connection-timeout: 30000
    idle-timeout: 600000
    max-lifetime: 1800000
  jpa:
    hibernate:
      ddl-auto: validate   # схему создаёт Flyway
    show-sql: true
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8081

app:
  jwt:
    secret: "change-me-please-change-me-please-32b+"  # потом надо бы заменить секрет для подписи токена
    ttl-minutes: 15 # время жизни JWT (увеличено с 2 до 15 для OTP флоу)
    refresh-ttl-days: 60
  otp:
    ttl-minutes: 5 # время жизни OTP
    sms-template: '${APP_OTP_SMS_TEMPLATE:OKO: Код подтверждения %s}'
  sms:
    enabled: ${APP_SMS_ENABLED:false}
exolve:
  base-url: https://api.exolve.ru
  api-key: ${EXOLVE_API_KEY:}
  sender: Okodoma
  telegram:
    enabled: ${APP_TELEGRAM_ENABLED:false}
    bot-token: ${APP_TELEGRAM_BOT_TOKEN:}
    chat-id: ${APP_TELEGRAM_CHAT_ID:}
  tinkoff:
    terminal-key: ${APP_TINKOFF_TERMINAL_KEY:1764771769246DEMO}
    password: ${APP_TINKOFF_PASSWORD:evjMB_tTiMh8rm9P}
  billing:
    public-base-url: ${APP_BILLING_PUBLIC_BASE_URL:https://pay.okodoma.ru}

nvr:
  base-url: ${NVR_BASE_URL:http://localhost:8082}