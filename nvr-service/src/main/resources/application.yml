server:
  port: 8082

spring:
  application:
    name: nvr-service

  datasource:
    url: jdbc:postgresql://89.23.102.97:5432/oko_api?sslmode=prefer
    username: oko_user
    password: Mx@:.s7T@#YUrg
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.jdbc.time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration

# ключ для CryptoService (сгенерируй: `openssl rand -base64 32`)
nvr:
  crypto:
    masterKeyBase64: "yn7vKJJS0NMYuQgUy3PpJPN9ZwevVH0cRZ0eV2H465U="
